/**
 * Mock recent activity — re-exported from dashboard for standalone use.
 */

import { mockDashboardSummary } from "./mock-dashboard";

export const mockRecentActivity = mockDashboardSummary.recentActivity;
